# Thunderbird — mail needs network by definition. Same shape as Helium
# (org.cicada.helium.env): NETWORK=allow, everything else stays off. Camera/
# mic/USB/sensors have no legitimate reason a mail client would need them.
NETWORK=allow
FILES=portal
CAMERA=deny
MIC=deny
USB=deny
SENSORS=deny
