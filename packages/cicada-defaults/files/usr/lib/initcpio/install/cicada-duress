#!/bin/bash
build() {
  add_runscript
}

help() {
  cat <<'EOF'
Cicada duress LUKS slot. Placeholder: does not alter cryptsetup yet.
Do not enable in mkinitcpio.conf until cicada-duress-enroll exists and tests pass.
EOF
}
